#!/usr/bin/python
NUMBER=1000
c = 0
for n in xrange(1,NUMBER):  # For each number between 1 and NUMBER:
    if n % 3 == 0 or n % 5 == 0:  # If that number is a multiple of 3 or 5:
        c += n  # Add the number to variable n
        print n, ':', c
